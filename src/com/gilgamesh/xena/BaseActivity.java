package com.gilgamesh.xena;

import android.app.Activity;

// All activities should inherit BaseActivity instead of Activity, so that common preferences may be set here.
public class BaseActivity extends Activity {
}
